#include <iostream>
using namespace std;

int n,ans,a[i],s;

int main(){
    std::ios::sync_with_stdio(false);
    cin>>n;
    for(int i=;i<n;++i) cin>>a[i];
    sort(a,a+n);
    for(int i=0;i<n;++i){
        s+=a[i];
        if(s>5)
    }
    cout<<ans;
    return 0;
}
